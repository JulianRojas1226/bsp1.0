html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Document
    
  body
    h2 Recuperar Contraseña
    p Ingresa tu correo para recibir un enlace de recuperación
    
    form(action="/solicitud" method="POST")
      input(type="email" name="email" placeholder="Correo electrónico" required)
      button(type="submit") Enviar Enlace